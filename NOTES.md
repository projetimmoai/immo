# Notes

Notes de travail libres — étapes à venir, idées, rappels. Pas un journal de
décisions techniques (ça, c'est dans les commits et le code) : plutôt un
pense-bête pour ne rien perdre entre deux sessions.

## Prochaines étapes

- Décidé : Gmail API (pas IMAP) pour la connexion à la boîte mail. Code déjà écrit : `internal/gmailapi` (client REST Gmail v1, auth OAuth2 "application installée" — un compte de service seul n'a pas accès à un Gmail personnel comme `projetimmoai@gmail.com`, contrairement à Drive), `internal/email` (enrichissement du contexte de l'expéditeur — personne/personne_physique/personne_morale, lots associés, contrats fournisseur — puis `DecideRoute` : décision de routage fondée sur ce contexte + des mots-clés dans l'objet/le corps, encore volontairement simple, `Decision.Action`). Reste à faire : créer un client OAuth2 "Application de bureau" (Google Cloud Console, API Gmail activée), lancer `go run ./cmd/gmail-auth` (autorisation ponctuelle dans un navigateur) et renseigner `GOOGLE_OAUTH_CLIENT_JSON`/`GOOGLE_OAUTH_TOKEN_JSON` dans `.env` (voir `.env.example`) ; puis implémenter la boucle de surveillance dans `cmd/worker-email` (récupération périodique via `gmailapi.ListMessageIDs`/`GetMessage`, dédup via `email.message_id`, insertion en base, appel à `email.TraiterMessage`, et le branchement de `Decision.Action` sur les fonctions métier correspondantes — création d'incident, de sinistre... — pas encore écrites).
- Table `categorie_email` renommée en `action` (racine de toute décision de routage, pas seulement les e-mails) ; `email.categorie_id` renommé en `email.action_id`. Nouvelle table `sous_action` (id, description, action_id FK -> action.id, auto-référencée via parent_id pour des sous-sous-actions, UNIQUE NULLS NOT DISTINCT(action_id, parent_id, description)) + `email.sous_action_id`, pour les sous-catégories à venir. En attente : la liste précise des actions/sous-actions (l'utilisateur va la fournir), à insérer en base puis à brancher dans `DecideRoute` — remplacement prévu de l'heuristique mots-clés par un appel à l'API Claude pour classifier le contenu (décision prise : garder les mots-clés pour l'instant, migration vers Claude à faire "bientôt").
- `internal/claudeapi` : client Claude (SDK Go officiel), modèle Haiku 4.5 (`ANTHROPIC_API_KEY` dans `.env`). `email.DetermineCopropriete` détermine à quelle copropriété un e-mail se rapporte à partir du `Contexte` enrichi : cas simple sans appel API si 0 ou 1 copropriété candidate au total (tous rôles confondus) ; sinon appel à `claudeapi.DecideCopropriete` (sortie structurée via un outil forcé — `tool_choice`, pas `output_config.format`) qui détermine le rôle probable et la copropriété, avec un indice de confiance (0 à 1) — la réponse est revalidée contre la liste des candidats fournis (jamais fait confiance à un `copropriete_id` inventé). **Interprétation à confirmer** : "cas facile" = exactement une copropriété candidate au total ; à corriger si ce n'est pas ce qui était voulu. Deux pièges d'API découverts en testant en direct : les schémas d'outils Claude n'acceptent pas `minimum`/`maximum` sur un type `number`, ni `enum` combiné à un `type` union `["string","null"]` (utiliser une valeur sentinelle string à la place de `null`).
- Connecter au Drive (compte de service Google). Code déjà écrit : `internal/drive` (client REST Drive v3), `internal/service.CoproprieteService.CreateCopropriete` (crée la copropriete en base + son arborescence Drive `<reference> - <nom>/{contrats,sinistres,incidents,emails,assemblees_generales}`, idempotent côté Drive). Reste à faire : créer le compte de service Google (Drive API activée), le partager en accès sur le dossier `NE PAS MODIFIER - fichiers`, renseigner `GOOGLE_SERVICE_ACCOUNT_JSON` et `DRIVE_ROOT_FOLDER_ID` dans `.env` (voir `.env.example`). Une fois fait, `internal/email` pourra aussi s'en servir pour les pièces jointes (`email_piece_jointe.url`).
- Lire ce fichier et créer les tables de comptabilité correspondantes dans la base de données : https://docs.google.com/spreadsheets/d/1FPkMkKSqHUX_XP8Cx6jFOJ8bF_il8lW2pVg4RJFvIcY

## Idées / à explorer plus tard

- Assemblée générale : modéliser (probablement plusieurs tables : l'AG elle-même, ses résolutions/votes, les présences/pouvoirs... à préciser) — rien n'existe pour l'instant, `exercice_comptable.ag_approbation_id` en attente.
- Renforcer l'intégrité référentielle restante si besoin (cf. discussion sur les FK).
- `internal/check.CheckCoproprietes` : vérifie la cohérence base/Drive, scope limité aux dossiers de copropriété pour l'instant. À étendre au fur et à mesure (nouvelles tables/dossiers Drive). Pas encore branché sur un cron — à faire quand `internal/email`/le worker seront en place (le cron pourra être un ticker dans `cmd/worker-email`, ou un endpoint dédié appelé par un scheduler externe).

## Vrac
