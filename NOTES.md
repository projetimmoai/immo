# Notes

Notes de travail libres — étapes à venir, idées, rappels. Pas un journal de
décisions techniques (ça, c'est dans les commits et le code) : plutôt un
pense-bête pour ne rien perdre entre deux sessions.

## Prochaines étapes

- Décidé : Gmail API (pas IMAP) pour la connexion à la boîte mail. Code déjà écrit : `internal/gmailapi` (client REST Gmail v1, auth OAuth2 "application installée" — un compte de service seul n'a pas accès à un Gmail personnel comme `projetimmoai@gmail.com`, contrairement à Drive), `internal/email` (enrichissement du contexte de l'expéditeur — personne/personne_physique/personne_morale, lots associés, contrats fournisseur, rôles). Reste à faire : créer un client OAuth2 "Application de bureau" (Google Cloud Console, API Gmail activée), lancer `go run ./cmd/gmail-auth` (autorisation ponctuelle dans un navigateur) et renseigner `GOOGLE_OAUTH_CLIENT_JSON`/`GOOGLE_OAUTH_TOKEN_JSON` dans `.env` (voir `.env.example`) ; puis implémenter la boucle de surveillance dans `cmd/worker-email` (récupération périodique via `gmailapi.ListMessageIDs`/`GetMessage`, dédup via `email.message_id`, insertion en base, orchestration `email.TraiterMessage` → `DetermineCopropriete` → `RouterEmail`).
- Table `categorie_email` renommée en `action` (racine de toute décision de routage, pas seulement les e-mails) ; `email.categorie_id` renommé en `email.action_id`. Nouvelle table `sous_action` (id, description, action_id FK -> action.id, auto-référencée via parent_id pour des sous-sous-actions, UNIQUE NULLS NOT DISTINCT(action_id, parent_id, description)) + `email.sous_action_id` — **ignorée pour l'instant** dans le routage (décision explicite : seulement l'action racine pour cette première version).
- `internal/claudeapi` : client Claude (SDK Go officiel), modèle Haiku 4.5 (`ANTHROPIC_API_KEY` dans `.env`). `email.DetermineCopropriete` détermine à quelle copropriété un e-mail se rapporte à partir du `Contexte` enrichi : cas simple sans appel API si 0 ou 1 copropriété candidate au total (tous rôles confondus) ; sinon appel à `claudeapi.DecideCopropriete` (sortie structurée via un outil forcé — `tool_choice`, pas `output_config.format`) qui détermine le rôle probable et la copropriété, avec un indice de confiance (0 à 1) — la réponse est revalidée contre la liste des candidats fournis (jamais fait confiance à un `copropriete_id` inventé). **Interprétation à confirmer** : "cas facile" = exactement une copropriété candidate au total ; à corriger si ce n'est pas ce qui était voulu. Deux pièges d'API découverts en testant en direct (schémas d'outils Claude) : `minimum`/`maximum` non supportés sur un type `number`, `enum` incompatible avec un `type` union `["string","null"]` (utiliser une valeur sentinelle string à la place de `null` — pas eu besoin de ce contournement pour `action`, `indetermine` étant déjà une vraie valeur de la table).
- **`email.RouterEmail`** (remplace l'ancien `DecideRoute` par mots-clés, supprimé) : une fois la copropriété identifiée (`ResolutionCopropriete.CoproprieteID` non nil), `NouveauContexteRoutage` construit un `domain.ContexteRoutage` (personne, rôle, copropriete, références des lots/numéros de contrats de cette personne dans cette copropriete précise), puis `RouterEmail` appelle `claudeapi.DecideAction` (même principe de sortie forcée par outil que `DecideCopropriete`, liste des actions chargée dynamiquement via `repository.ListActions` — jamais codée en dur) pour classifier le contenu parmi les actions existantes, revalide la réponse, puis appelle la fonction de traitement correspondante. **Un fichier Go par action** (`internal/email/action_*.go`), chacune une fonction vide pour l'instant (`// TODO: implémenter`), pour développer la logique métier de chacune indépendamment sans un fichier monstre. Table `action` actuelle (10 lignes, `indetermine` a disparu — il n'y a donc plus de valeur de repli dédiée, le prompt Claude demande de choisir la plus proche et de refléter l'incertitude dans `confiance`) : `sinistre`, `incident`, `assemblee_generale`, `autre`, `comptabilite`, `mutation`, `contentieux`, `travaux`, `controle_gestion`, `demande_administrative` — un fichier `action_*.go` par valeur, tenu à jour manuellement si la table change encore. `TraiterMessage` a été simplifié : il ne fait plus que `EnrichirExpediteur` (l'orchestration complète — `DetermineCopropriete` puis `RouterEmail` — a trop de dépendances propres à chaque étape pour tenir dans un seul helper ; à faire dans le futur worker). Reste à faire : consigner `action_indeterminee` dans `log` quand la confiance de `RouterEmail` est faible (même principe que `copropriete_non_identifiee`, pas encore câblé), puis implémenter chaque `action_*.go` un par un.
- Connecter au Drive (compte de service Google). Code déjà écrit : `internal/drive` (client REST Drive v3), `internal/service.CoproprieteService.CreateCopropriete` (crée la copropriete en base + son arborescence Drive `<reference> - <nom>/{contrats,sinistres,incidents,emails,assemblees_generales}`, idempotent côté Drive). Reste à faire : créer le compte de service Google (Drive API activée), le partager en accès sur le dossier `NE PAS MODIFIER - fichiers`, renseigner `GOOGLE_SERVICE_ACCOUNT_JSON` et `DRIVE_ROOT_FOLDER_ID` dans `.env` (voir `.env.example`). Une fois fait, `internal/email` pourra aussi s'en servir pour les pièces jointes (`email_piece_jointe.url`).
- Lire ce fichier et créer les tables de comptabilité correspondantes dans la base de données : https://docs.google.com/spreadsheets/d/1FPkMkKSqHUX_XP8Cx6jFOJ8bF_il8lW2pVg4RJFvIcY
- Tables `log`/`log_type` créées (évènements/anomalies consignés, avec `email_id`/`copropriete_id`/`personne_id` optionnels). `log_type` initial : `copropriete_non_identifiee`, `expediteur_inconnu`, `action_indeterminee`, `confiance_faible`, `erreur_appel_claude`, `desynchronisation_base_drive` — liste à ajuster/compléter selon les besoins réels. Branché dans `email.DetermineCopropriete` (`copropriete_non_identifiee` si confiance < 0.8). Reste à faire : `action_indeterminee` (dans `action_indetermine.go`) et `desynchronisation_base_drive` (dans `service.CreateCopropriete`, actuellement seulement un `log.Printf`).

## Idées / à explorer plus tard

- Assemblée générale : modéliser (probablement plusieurs tables : l'AG elle-même, ses résolutions/votes, les présences/pouvoirs... à préciser) — rien n'existe pour l'instant, `exercice_comptable.ag_approbation_id` en attente.
- Renforcer l'intégrité référentielle restante si besoin (cf. discussion sur les FK).
- `internal/check.CheckCoproprietes` : vérifie la cohérence base/Drive, scope limité aux dossiers de copropriété pour l'instant. À étendre au fur et à mesure (nouvelles tables/dossiers Drive). Pas encore branché sur un cron — à faire quand `internal/email`/le worker seront en place (le cron pourra être un ticker dans `cmd/worker-email`, ou un endpoint dédié appelé par un scheduler externe).

## Vrac
